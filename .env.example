PORT=3000
JWT_SECRET=replace-with-a-long-random-secret

# Railway PostgreSQL sets DATABASE_URL automatically when a database is attached.
# Leave this unset for local JSON-backed development storage.
DATABASE_URL=

# Set to "require" only if your external PostgreSQL provider requires SSL.
PGSSLMODE=
